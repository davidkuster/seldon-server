#!/bin/bash

set -o nounset
set -o errexit

K8S_NAMESPACE=${K8S_NAMESPACE:-default}

STARTUP_DIR="$( cd "$( dirname "$0" )" && pwd )"

function seldon_down {
    kubectl --namespace=${K8S_NAMESPACE} delete pods,services,deployments,replicationcontrollers -l service=seldon
}

seldon_down "$@"
